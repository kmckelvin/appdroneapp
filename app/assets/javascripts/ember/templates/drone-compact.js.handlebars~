{{#with drone}}
  <h2>{{name}}</h2>

  {{#each params}}
    <div class="param">
      {{#if isBooleanParam}}
        {{view App.BooleanParamView contentBinding="this"}}
        <span class="hint">{{opts.info}}</span>
        <br />
      {{/if}}
      {{#if isChooseManyParam}}
        {{view App.ChooseManyParamView contentBinding="this"}}
      {{/if}}
    </div>
  {{/each}}

{{/with}}
