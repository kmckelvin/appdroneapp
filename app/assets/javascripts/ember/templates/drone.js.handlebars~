<section {{bindAttr class="droneClass"}}>
  <h3>
    <button {{bindAttr class="toggleButtonClass"}} {{action "toggle"}}>
      <i {{bindAttr class="toggleIconClass"}}></i>
    </button>

    &nbsp;

    <span class="name">{{content.name}}</span>

    <span class="stats">
      {{#if content.hasActiveDependants}}
        <i class='icon-lock'>
          {{content.activeDependantCount}}
        </i>
      {{/if}}

      {{#if content.hasDependencies}}
        <i class="icon-external-link" rel="tooltip" title="tooltip">
          {{content.activeDependencyCount}}/{{content.dependencyCount}}
        </i>
      {{/if}}

      {{#if content.hasPairs}}
        <i class="icon-heart" rel="tooltip" title="tooltip">
          {{content.pairCount}}
        </i>
      {{/if}}

      {{#if content.hasParams}}
        <i class="icon-list" rel="tooltip" title="tooltip">
          {{content.paramCount}}
        </i>
      {{/if}}
    </span>

    <small class="category pull-right">
      {{content.category.name}}
    </small>
  </h3>

  <p class="desc">
    {{content.desc}}
  </p>

</section>

